import { Router } from "express";
import { authenticate } from "../middleware/auth.js";

const router = Router();

// TODO: Implement waitlist endpoints (join, leave, view position)

export default router;
